
///Ответ на A
да/нет(на цикл):
	итераторы a и b
	пока(тру):
		если a+1 существует то a=a+1, иначе возврат фалс

		если b+1 существует то b=b+1, иначе возврат фалс
		если b+1 существует то b=b+1, иначе возврат фалс		

		если a=b, возврат тру


///Ответ на B. учитывая, что итератор b мы не трогаем
инт i=1
a=1
пока a!=b:
	a++
	i++
принт i

Придумал две теории. 
Первое что заметил, мне кажется самое хорошее решение
1)Оставляем так-же указатель b на месте
a -> to head of List
While a!=b:
	a++
	b++
Точка их встречу и будет началом списка.
-------
Второе что придумал, как раз основываясь на том, что ты подсказал(почему-то осознание пришло только после нахождения первого решения)
2)a -> to head of list
b -> move = 2*(i-1)(Это у нас берётся длина нашего лупа
while a!=b:
	a++
	b++
	if b!=a, b++
	

